# Lot LOT-001 — Project setup (Poetry + src/ structure)

Status: ✅ done

**Effort**: S
**Branch**: feature/lot-001-project-setup → PR → develop

| # | Ticket | Type | Status |
|---|--------|------|--------|
| CHOR-001 | Initialise `pyproject.toml` with Poetry — runtime + dev dependencies (fastapi, uvicorn, textual, mcp, pydantic, pyyaml), ruff/mypy/pytest config, and the `dcs-serve` / `dcs-client` entry points. | chore | ✅ |
| CHOR-002 | Create package structure `src/dcs_bridge/` with sub-packages `serve/`, `client/tui/`, `client/web/static/`, `client/mcp/`, `common/`. All packages importable via `importlib` mode in pytest. | chore | ✅ |
| CHOR-003 | Create `test/` directory — empty `test/__init__.py` marking the package; test files follow the `test_*.py` convention. | chore | ✅ |
| CHOR-004 | Configure ruff (`line-length = 120`, rules `E/F/W/I/UP`, `ruff format` canonical), mypy (`check_untyped_defs`, `ignore_missing_imports`), pytest (`asyncio_mode = auto`, `pytest-cov`, `--import-mode=importlib`). | chore | ✅ |
| CHOR-005 | Add `.editorconfig` — UTF-8, LF line endings, 4-space indentation for all contributors. | chore | ✅ |
