# Lot LOT-007 — dcs-client --mcp (MCP server)

Status: ✅ done

**Effort**: M
**Branch**: feature/lot-007-mcp → PR → develop

| # | Ticket | Type | Status |
|---|--------|------|--------|
| FEAT-028 | Tool `exec_lua(code: str, timeout: float \| None = None) -> str` — calls `POST /api/exec`; returns the result string, raises `McpError` on DCS error / 503 / 504. For AI agents querying or mutating DCS state with arbitrary Lua. | feat | ✅ |
| FEAT-029 | Tool `get_units() -> list[dict]` — calls `GET /api/units`; returns the full unit list (`Unit` schema), raises `McpError` on 503. Gives the agent battlefield situational awareness. | feat | ✅ |
| FEAT-030 | Tool `spawn_unit(group_def: dict) -> str` — calls `POST /api/spawn`; returns the spawned group id, raises `McpError` on 503/504 or Lua error. Agent supplies a valid DCS group definition (no server-side schema validation beyond `coalition.addGroup()`). | feat | ✅ |
| FEAT-031 | Tool `get_mission_info() -> dict` — calls `GET /api/mission`; returns `{"theatre": "<name>"}`. Lightweight orientation probe; extend alongside FEAT-018. | feat | ✅ |
| TEST-004 | Unit tests for MCP tools — mock `httpx.AsyncClient` at transport level; assert URL, `X-API-Key` header, JSON body per tool; cover success, DCS error in body, 503 → `McpError`, 504 → `McpError`. | test | ✅ |
