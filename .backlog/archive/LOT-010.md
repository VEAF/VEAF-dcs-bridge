# Lot LOT-010 — Documentation (MkDocs + GitHub Pages)

Status: ✅ done

**Effort**: L
**Branch**: feature/lot-010 → PR → develop

| # | Ticket | Type | Status |
|---|--------|------|--------|
| DOCS-001 | MkDocs setup — `mkdocs-material`, `mkdocs-static-i18n` (suffix mode: `page.fr.md`/`page.en.md`), `mike` (versioned docs). Default language FR, EN built in parallel. New `[tool.poetry.group.docs.dependencies]`; `docs/` at the repo root. | doc | ✅ |
| DOCS-002 | GitHub Actions `docs.yml` — triggered on push to `develop` (alias `dev`) and `master` (alias `latest`); publishes to `gh-pages` via `mike`; git identity set for the bot. | doc | ✅ |
| DOCS-003 | User documentation (FR default + EN) — two targets (mission maker, server owner): prerequisites (DCS World, VMCT v6), installation (GitHub Releases .exe), configuration (`dcs-serve.yaml`/`dcs-client.yaml`), quick start, CLI reference. | doc | ✅ |
| DOCS-004 | Technical documentation (FR default + EN) — architecture (Mermaid: Lua → TCP → dcs-serve → HTTP/WS → dcs-client), repo structure, wire protocol (newline-delimited JSON over TCP), contributing guide (Poetry, TDD, Conventional Commits, PR flow), REST + WebSocket API reference. | doc | ✅ |
| DOCS-005 | README.md rewrite (~1 page) — pitch, architecture diagram, feature bullets, 3-command quick start, link to online docs (GitHub Pages), VMCT v6 note (https://veaf.github.io/documentation/dev/). | doc | ✅ |
