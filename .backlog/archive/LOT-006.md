# Lot LOT-006 — dcs-client --tui (Textual)

Status: ✅ done

**Effort**: M
**Branch**: feature/lot-006-tui → PR → develop

| # | Ticket | Type | Status |
|---|--------|------|--------|
| FEAT-024 | Unit snapshot display — `DataTable` (Name, Type, Coalition, Lat, Lon, Alt m), replaced entirely on each `full_refresh` WebSocket message. | feat | ✅ |
| FEAT-025 | Arbitrary Lua input + result display — `Input` submits code via `on_input_submitted`, `POST /api/exec` via `httpx.AsyncClient`; result green / error red in a `RichLog`. | feat | ✅ |
| FEAT-026 | WebSocket connection for real-time updates — `run_worker()` coroutine to `WS /ws/stream`, reconnects every 5 s on `WebSocketException`; status shown in a docked `Label`. | feat | ✅ |
| FEAT-027 | `dcs-client.yaml` config — `ClientConfig` Pydantic model (`host`, `port`, `api_key`); `load_config()` falls back to defaults on missing/invalid/non-mapping content with a warning. Entry: `dcs-client tui --config <path>`. | feat | ✅ |
