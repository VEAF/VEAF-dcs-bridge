# Lot LOT-002 — Shared types (common/)

Status: ✅ done

**Effort**: S
**Branch**: feature/lot-002-shared-types → PR → develop

| # | Ticket | Type | Status |
|---|--------|------|--------|
| TECH-001 | Pydantic models `Unit`, `UnitPositionDcs`, `UnitPositionGeo`, `Command`, `Response`, `DcsEvent`, `FullRefresh` — frozen (`ConfigDict(frozen=True)`) wire-format types shared across the Lua bridge, `dcs-serve`, and clients. `Response` invariant: exactly one of `result`/`error` non-None, enforced via `@model_validator(mode="after")`. | tech | ✅ |
| TECH-002 | Protocol enums — `CommandAction` (`exec`, `spawn`), `EventName` (`unit_position`, `unit_destroyed`, `unit_spawned`), `Coalition` (`IntEnum`: 0 Neutral / 1 Red / 2 Blue, matching DCS scripting values). Protocol: newline-delimited JSON over TCP, one message per `\n`-terminated line. | tech | ✅ |
| TEST-001 | Unit tests for all models — valid construction, frozen immutability, `Response` invariant enforcement, enum value round-trips. | test | ✅ |
