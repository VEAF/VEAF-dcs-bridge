# Lot LOT-011 — Typer entry-point fixes

Status: ✅ done

**Effort**: XS
**Branch**: fix/typer-entry-point → PR #13 → develop

| # | Ticket | Type | Status |
|---|--------|------|--------|
| BUGF-001 | Typer entry-point crash on `dcs-serve` startup — in `serve/app.py`, `main()` was both the Typer-decorated command and the Poetry entry point; called directly by Poetry, Typer never parsed argv, so `config` received the raw `OptionInfo` and `config.exists()` raised `TypeError: 'bool' object is not callable`. Fixed by splitting `_command()` from a thin `main()` that calls `_cli()`. | fix | ✅ |
| BUGF-002 | `typer.Option(Path(...))` pattern in `dcs-client` subcommands — all three (`tui`, `web`, `mcp`) used `typer.Option(Path("dcs-client.yaml"), ...)`. Replaced with `typer.Option(default="dcs-client.yaml", ...)` so Typer converts the string to `Path` via the annotation, avoiding the OptionInfo-as-default pitfall. | fix | ✅ |
