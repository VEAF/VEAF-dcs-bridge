# Lot LOT-008 — dcs-client --web (static HTTP + Leaflet)

Status: ✅ done

**Effort**: M
**Branch**: feature/lot-008-web → PR → develop

| # | Ticket | Type | Status |
|---|--------|------|--------|
| FEAT-032 | Local static HTTP server — `StaticFiles` serving `client/web/static/` on a configurable local port (default 8081). Uvicorn launched in a thread so the main thread can open the browser. No CORS needed (page uses the full dcs-serve URL). | feat | ✅ |
| FEAT-033 | Automatic browser open — `webbrowser.open(...)` 500 ms after Uvicorn readiness (threading.Event), avoiding opening before the server accepts connections; graceful fallback when headless. | feat | ✅ |
| FEAT-034 | Leaflet map with units coloured by coalition — single-page HTML/JS (Leaflet from CDN, no build step), OpenStreetMap tiles. Each unit a `L.circleMarker`; colours Neutral `#888` / Red `#c0392b` / Blue `#2980b9`; radius scaled by category. | feat | ✅ |
| FEAT-035 | Hover tooltip — `L.popup` per marker: `<b>{name}</b><br>{type}<br>Alt: {altitude_agl:.0f} m AGL`. Opens on `mouseover`, closes on `mouseout`; click pins it open. | feat | ✅ |
| FEAT-036 | WebSocket connection for real-time updates — JS `WebSocket` to `{host}:{port}/ws/stream?api_key={key}`. `full_refresh` clears + re-adds markers; `unit_position`/`unit_spawned` upsert by name; `unit_destroyed` removes by name. Reconnect 5 s; status badge top-right. | feat | ✅ |
