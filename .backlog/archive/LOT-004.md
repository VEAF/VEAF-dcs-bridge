# Lot LOT-004 — dcs-serve core (TCP asyncio + snapshot)

Status: ✅ done

**Effort**: L
**Branch**: feature/lot-004-serve-core → PR → develop

| # | Ticket | Type | Status |
|---|--------|------|--------|
| FEAT-009 | Asyncio TCP handler — `run_tcp_server()` via `asyncio.start_server()` on `tcp_host:tcp_port`; accepts exactly one DCS connection, logs and resets on disconnect. | feat | ✅ |
| FEAT-010 | Newline-delimited JSON parser — reads lines via `StreamReader.readline()`, UTF-8 decode + JSON parse. Dispatch: `full_refresh`/event types → `Snapshot`; messages with `id` → `CommandBus` as `Response`. Malformed lines logged as warnings and discarded without crashing. | feat | ✅ |
| TECH-003 | In-memory Snapshot (ADR-0002) — replaced atomically on each `full_refresh`; `stale()` returns `True` when the last update is older than `stale_threshold`. | tech | ✅ |
| FEAT-011 | Snapshot update via Events + Full Refresh — `full_refresh` replaces the `dict[str, Unit]` atomically; `unit_position` upserts; `unit_spawned` inserts; `unit_destroyed` removes (silently ignored if unknown). All mutations flow through one method to keep `last_updated` consistent. | feat | ✅ |
| FEAT-012 | `ready` / `stale` state management — three-state model (not ready → 503; ready+fresh → normal; ready+stale → 503 with `{"stale": true}`, exec still forwarded). `stale()` uses `time.monotonic()` to avoid wall-clock drift; default threshold 15 s. | feat | ✅ |
| FEAT-013 | Command/Response correlation by `id` — `CommandBus.register(id)` creates an `asyncio.Event` slot; `wait(id, timeout)` blocks until reply/timeout; `unregister(id)` cleans up on send failure. | feat | ✅ |
| FEAT-014 | DCS disconnection handling — on TCP disconnect: `connected = False`, in-flight commands unregistered, snapshot marked stale; API returns 503 until reconnect. | feat | ✅ |
| TEST-002 | Integration tests with a fake DCS TCP server (pytest fixture) — covers reconnect, stale detection, timeout, command/response correlation. | test | ✅ |
