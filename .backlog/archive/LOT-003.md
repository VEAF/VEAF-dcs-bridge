# Lot LOT-003 — Lua bridge script

Status: ✅ done

**Effort**: M
**Branch**: feature/lot-003-lua-bridge → PR → develop

| # | Ticket | Type | Status |
|---|--------|------|--------|
| FEAT-001 | Outgoing TCP connection to dcs-serve — `socket.connect(host, port)` on startup, non-blocking to avoid stalling the simulation thread. | feat | ✅ |
| FEAT-002 | Exponential backoff reconnect — starts at 1 s, doubles per failure, capped at 30 s; warning logged after 60 s of continuous disconnection (ADR-0003). | feat | ✅ |
| FEAT-003 | Non-blocking socket (`settimeout(0.0001)`) — never blocks the simulation thread (Lua bridge runs on the DCS scheduler ~20 Hz). Partial reads buffered and flushed line by line on `\n`. | feat | ✅ |
| FEAT-004 | Full Refresh every 5 s — serialises all alive units with DCS-native (`x/y/z`) and geographic (`lat/lon`) coordinates plus AGL altitude from `land.getHeight()`, sent as a `full_refresh` message. | feat | ✅ |
| FEAT-005 | Spontaneous events — `unit_position`, `unit_destroyed`, `unit_spawned` pushed via DCS event handlers (`world.addEventHandler`), each a JSON line `{"type": "<event>", "data": {...}}`. Position/spawned carry the full `Unit` payload; destroyed carries only `name`. | feat | ✅ |
| FEAT-006 | Coordinate conversion — `coord.LOtoLL()` (flat-earth → lat/lon) and `land.getHeight(x, z)` for AGL (ADR-0001). | feat | ✅ |
| FEAT-007 | Command execution — `exec` runs arbitrary Lua via `load()` and returns the string result; `spawn` calls `coalition.addGroup()` with a JSON group definition table. | feat | ✅ |
| FEAT-008 | Injection modes — persistent via MissionScripting.lua, and per-mission via DO SCRIPT FILE trigger (no DCS install modification). | feat | ✅ |
