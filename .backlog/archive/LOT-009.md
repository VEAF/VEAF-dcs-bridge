# Lot LOT-009 — Packaging (PyInstaller + CI GitHub Actions)

Status: ✅ done

**Effort**: M
**Branch**: feature/lot-009-packaging → PR → develop

| # | Ticket | Type | Status |
|---|--------|------|--------|
| RELE-001 | `dcs-serve.spec` PyInstaller — one-file executable; bundles the `dcs-serve.yaml` default template and the Lua script as data files. | release | ✅ |
| RELE-002 | `dcs-client.spec` PyInstaller — one-file executable; bundles the `dcs-client.yaml` default template. | release | ✅ |
| RELE-003 | GitHub Actions `release.yml` — triggered on `v*` tags: checkout → Poetry install → ruff + mypy + pytest → PyInstaller build (Linux/Windows/macOS matrix) → upload artifacts → `gh release create`. | release | ✅ |
| RELE-004 | PyPI publication — `poetry publish` step in `release.yml`; enables `pip install dcs-bridge` alongside binary releases. | release | ✅ |
