# Lot LOT-005 — dcs-serve API (FastAPI + WS + auth)

Status: ✅ done

**Effort**: L
**Branch**: feature/lot-005-serve-api → PR → develop

| # | Ticket | Type | Status |
|---|--------|------|--------|
| FEAT-015 | `POST /api/exec` — forwards Lua to `CommandBus`, waits for `Response`. 503 if DCS disconnected, 504 on timeout, 200 with `{"result": ...}`/`{"error": ...}` otherwise. Global timeout + per-request override. | feat | ✅ |
| FEAT-016 | `POST /api/spawn` — sends a `spawn` command with `group_def` (DCS group definition format) to the Lua bridge (`coalition.addGroup()`). Same 503/504/200 logic; result is the spawned group id. | feat | ✅ |
| FEAT-017 | `GET /api/units` — returns the current Snapshot as a JSON array; 503 if not ready or stale. | feat | ✅ |
| FEAT-018 | `GET /api/mission` — runs `pcall(function() return env.mission.theatre end)` via FEAT-015, returns `{"result": "<theatre>"}` (e.g. Caucasus, Syria, PersianGulf). Extendable to richer mission metadata. | feat | ✅ |
| FEAT-019 | `WS /ws/stream` — authenticates on connect, sends current snapshot immediately if ready, then subscribes to `EventBroadcaster` and forwards all messages until disconnect. | feat | ✅ |
| FEAT-020 | API Key middleware — `Depends(_require_api_key)` on all HTTP routes; checks `X-API-Key` header or `api_key` query param. | feat | ✅ |
| FEAT-021 | Automatic key generation — if `api_key` absent in `dcs-serve.yaml`, generate `secrets.token_urlsafe(32)` and persist to the file on first start. | feat | ✅ |
| FEAT-022 | `dcs-serve.yaml` config — `ServeConfig` Pydantic model: `tcp_host`, `tcp_port`, `http_host`, `http_port`, `api_key`, `default_timeout`, `stale_threshold`. | feat | ✅ |
| FEAT-023 | Correct HTTP codes — 200 success (even on Lua error: the bridge responded), 503 DCS not connected/stale, 504 command timeout (per ADR). | feat | ✅ |
| TEST-003 | FastAPI tests with `httpx.AsyncClient` (ASGI transport) — auth, all endpoints, WS stream, stale/disconnected states. | test | ✅ |
